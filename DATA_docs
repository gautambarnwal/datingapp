==============Credentials===============




==============Credentials close===============



==============git commands ===============

github
https://github.com/1998gtb/datingapp.git

<!-- for fist time setup-->
# git init
# git add README.md
# git status

# git add .                              //git add all
# git status
# git commit -m "initial commit"         //git commit with message initial commit

# git remote add origin https://github.com/1998gtb/datingapp.git

# git push -u origin master
	OR
# git push -f origin master               // —force pushing



<!-- for every time push-->
# git status                         // check somo changes are there or not  
# git add .                          // add all to commit
# git status	                     // what are Changes to be committed:
# git commit -m "created models"     // commiting with message "created models"
# git status                         // Checking working tree clean or not
# git push -u origin master          // push to master branch{ we have added origin earlier}
==============git commands end===============








# php artisan make:model Model/Profile -a
# php artisan make:model Model/Image -a
# php artisan make:model Model/Like -a
# php artisan make:model Model/Chat -a

# php artisan route:list



--------

# composer require laravel/ui

# php artisan ui bootstrap --auth       //Using Bootstrap: (i used this)

# php artisan ui vue --auth             // Using Vue:

# php artisan ui react --auth           //  Using React:

# npm install                           // Install NPM:
# npm run dev                           //Run NPM: 

-- Remove vue and vue-template-compiler from the devDependencies in your package.json file.
-- Run npm update     -- Remove the following code from your app.js file...
-- "vue-template-compiler": "^2.6.11"	



# php artisan db:seed

# php artisan tinker
 >>> App\User::find(64)->images
 >>> App\User::find(64)
 >>> App\User::find(64)->profile


// passport config ---- https://laravel.com/docs/7.x/passport
# composer require laravel/passport
# php artisan migrate
# php artisan passport:install

